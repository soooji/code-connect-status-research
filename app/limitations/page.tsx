import type { Metadata } from 'next'
import { ComparisonTable } from '@/components/comparison-table'
import { Callout, PageHeader, Section } from '@/components/page'
import { limitationGroups,replacementRows } from '@/data/limitations'
export const metadata:Metadata={title:'Limitations'}
export default function Limitations(){return <div className="container"><PageHeader eyebrow="Decision guide" title="Know what you are adopting—and what you are not." description="Code Connect is a high-leverage mapping and context layer. It is not synchronization, documentation, testing, or an application generator. Plan for ownership before scaling."/><div className="decision-banner"><div><span>Good fit</span><strong>Stable component APIs, clear design parity, and an owned design system</strong></div><div><span>Proceed carefully</span><strong>Frequent drift, free-form layers, or no team accountable for mappings</strong></div></div><Section eyebrow="Boundaries by layer" title="Four places where expectations matter"><div className="limitation-groups">{limitationGroups.map((g,i)=><article key={g.title}><div className="limit-title"><span>0{i+1}</span><div><small>{g.eyebrow}</small><h3>{g.title}</h3></div></div><ul>{g.items.map(x=><li key={x}>{x}</li>)}</ul></article>)}</div></Section><Section eyebrow="Replacement matrix" title="What Code Connect does not replace"><ComparisonTable rows={replacementRows}/></Section><Section eyebrow="Rollout recommendation" title="Treat mappings as maintained product code"><div className="rollout-list">{[['Own','Name design-system owners for template changes and publishing.'],['Validate','Parse and preview mappings in CI before releasing them.'],['Observe','Test Dev Mode and MCP output in real implementation tasks.'],['Review','Update mapping contracts with design and component API changes.']].map(([n,d],i)=><article key={n}><span>{i+1}</span><h3>{n}</h3><p>{d}</p></article>)}</div><Callout title="Verify organization constraints">Figma plans, seats, library publishing, Dev Mode access, tokens, and GitHub integration can change. Confirm current official entitlement and security guidance before committing to a rollout.</Callout></Section></div>}
