export function ComparisonTable({rows}:{rows:readonly (readonly string[])[]}){return <div className="table-wrap"><table><thead><tr><th>Need</th><th>Code Connect?</th><th>Use instead / also</th></tr></thead><tbody>{rows.map(r=><tr key={r[0]}>{r.map((c,i)=><td key={c} data-label={['Need','Code Connect?','Use instead / also'][i]}>{i===1?<span className={`answer ${c.toLowerCase()}`}>{c}</span>:c}</td>)}</tr>)}</tbody></table></div>}
